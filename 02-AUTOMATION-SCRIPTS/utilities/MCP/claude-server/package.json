{
  "name": "claude-server",
  "version": "0.1.0",
  "description": "MCP server for Claude Opus 4.5 and subagents",
  "type": "module",
  "scripts": {
    "build": "tsc && node -e \"import('fs').then(fs => fs.chmodSync('build/claude-server/index.js', '755'))\"",
    "start": "node build/index.js"
  },
  "dependencies": {
    "openai": "^4.67.3",
    "@modelcontextprotocol/sdk": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}